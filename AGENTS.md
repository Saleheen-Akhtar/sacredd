# Sacred Kompass v10 — Agent Notes
   This is a Gutenberg-block WordPress theme rebuilt from scratch.
   - Tokens live in `theme.json` and `assets/css/tokens.css`.
   - Each section = one block in `/blocks/<slug>/`.
   - Animation engine: GSAP + ScrollTrigger + Lenis (NEVER jQuery, NEVER add Three.js without approval).
   - Read `JULES_TASK.md` for the full spec.
